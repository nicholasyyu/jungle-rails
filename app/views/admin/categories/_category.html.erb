<tr>
    <td>
      	<h4><%= category.name %></h4>
    </td>

    <td>
    	<%= link_to fa_icon('trash'), admin_category_path(category), class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: "Are you sure?" } %>
  	</td>
</tr> 

<!-- admin_category_path -->